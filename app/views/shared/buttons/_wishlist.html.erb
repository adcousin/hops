<!-- Version 2 : Add parameter in link_to -->
<% if !active_core_list.nil? && active_core_list.name == 'Wishlist' && active_core_content %>
  <%= link_to image_tag('icons/wishlist-white.svg', width: 20, height: 20), list_content_path(wishlist, active_core_content, page_tracker: "beer_show"), method: 'delete', class: "button active", remote: true %>
<% else %>
  <%= link_to image_tag('icons/wishlist-black.svg', width: 20, height: 20), list_contents_path(wishlist, beer_id: beer.id, user_core_lists: user_core_lists), method: 'post', class: "button", remote: true %>
<% end %>
