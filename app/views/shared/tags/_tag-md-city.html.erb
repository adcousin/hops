<%= link_to searches_path(:query => "#{@beer.brewery.city}") do %>
  <div class="tag-md">
    <div class="tag-icon-md city">
      <i class="fas fa-city"></i>
    </div>
    <div class="tag-name-md">
      <p> <%= @beer.brewery.city %> </p>
    </div>
  </div>
<% end %>